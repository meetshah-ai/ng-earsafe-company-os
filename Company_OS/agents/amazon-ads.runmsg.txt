Run this week's Amazon Ads Operator cycle for D = today.

Read ONLY: Company_OS/departments/amazon/tracker.md, Company_OS/departments/amazon/learning-log.md, and the tail of Company_OS/APPROVALS_QUEUE.md (to find the highest existing AZ-### so you don't collide with a number already used). Do NOT read COMPANY_STATE.md, another department, the decision log, or a past report.

Pull directly from the Amazon Ads API (no Windsor, no MCP dependency) per the "HOW TO REACH THE AMAZON ADS API" section of your system prompt: exchange the refresh token for an access token, then pull campaign-level performance for both accounts (NG Corporation profile 1452172411967063, NG AMS profile 1498424255518113) across Sponsored Products, Sponsored Brands and Sponsored Display via the async Reporting API (request, poll, download) — spend, 14-day attributed sales, clicks, impressions, orders, 30-day window. Also pull the SP search-term/targeting report for harvest and negative-prune candidates. Read the real field names the API returns — do not assume they match the guessed shape in your system prompt or Windsor's old amazon_ads schema.

Compute, in one pass: per-campaign ad-attributed ROAS (matured 14-day window only — flag anything inside the last 14 days as immature and do not act on it), ACOS, CTR, CPC, CVR, week-over-week deltas, and a SCALE / HOLD / FIX-KILL classification per campaign against the campaign scale test (incremental spend only to campaigns with healthy ROAS AND evidence it holds at higher spend).

Produce all four verdicts — CUT, FIX, SCALE-or-KILL, TEST — each grounded in a number from this session. Hold the ad-attributed ROAS floor at 6.0: scale only while marginal ROAS on the last increment stays ≥ 6.0, in ≤20% steps, with a 2-week no-touch window after each step. Non-budget optimization (negatives, search-term harvests, bid tweaks) is never gated by the ratchet — always propose it. Never report the department's blended P&L ROAS (the ≥15 bar) as if it were this agent's floor. Never call anything but a GET request or the /reporting/reports POST — no campaign, budget, bid, or account mutation of any kind.

Then commit the report to Company_OS/amazon-ads/<D>.md on main (no branch, no PR), append AZ-### draft rows to Company_OS/APPROVALS_QUEUE.md per the department's brief standard (hypothesis + single variable + past-data signal or NET-NEW flag + expected impact + read plan with a kill/scale rule), log the cycle to Company_OS/departments/amazon/learning-log.md, and update Company_OS/departments/amazon/tracker.md.
