# Amazon Ads Operator — Weekly Cycle — 2026-08-10

**Accounts in scope:** NG Corporation / SC (profile `1452172411967063`), NG AMS / VC (profile `1498424255518113`).
**Floor this agent owns:** ad-attributed ROAS ≥ 6.0 (14-day matured, per campaign, rolled to SP/SB/SD and account). *This is not the department's blended P&L ROAS ≥ 15 — that bar belongs to `amazon.md` / the constitution and is not restated here.*

## SESSION STATUS: BLOCKED — Amazon Ads API rejected every data call

No campaign, ad group, keyword, budget, bid, spend, sales, or search-term number was successfully retrieved this session. Every verdict below is therefore built from (a) this session's own diagnostic findings, which are real numbers pulled today, and (b) explicitly-labeled carried-forward facts from the 2026-07-08 cycle log — never presented as fresh data.

### What was attempted (in order)
1. `POST https://api.amazon.com/auth/o2/token` (refresh_token grant) — **succeeded, HTTP 200**, three separate times across this session, each returning a fresh `access_token` (60-min bearer, `token_type: bearer`, a `refresh_token` echoed back). LWA itself accepts `AMZADS_CLIENT_ID` + `AMZADS_CLIENT_SECRET` + `AMZADS_REFRESH_TOKEN` as a valid triple.
2. `POST https://advertising-api-eu.amazon.com/reporting/reports` (SP campaign report request, NG Corporation profile `1452172411967063`) — **HTTP 401**: `{"message":"Unauthorized exception while handling 3P Request: clientId does not match token"}`.
3. `GET https://advertising-api-eu.amazon.com/v2/profiles` (basic profile list, no scope header needed) — **HTTP 401**, generic `{"code":"UNAUTHORIZED"}` — with the `Amazon-Advertising-API-ClientId` header, without it, and with a `Content-Type` header added. All three variants failed identically.
4. `POST https://advertising-api.amazon.com/reporting/reports` (NA host, diagnostic only — **not** used for the "real" pull, per this agent's standing instruction to use the EU host for this India account) — **HTTP 401**, same exact `clientId does not match token` message. This rules out an EU/NA host-routing explanation.
5. Reporting call repeated with the `Amazon-Advertising-API-Scope` header removed entirely — same exact error. Rules out a profile-scope problem.
6. Full end-to-end retest in a single shell command (mint token → immediately use it) to rule out token staleness/expiry between calls — same exact error.

**Conclusion:** this is a credential-pairing failure between `AMZADS_CLIENT_ID` and the token minted from `AMZADS_REFRESH_TOKEN`/`AMZADS_CLIENT_SECRET`, reproducible regardless of host, endpoint, or scope header. The LWA token endpoint accepts the triple; the Ads API resource servers reject the resulting token against that client ID, every time. **This is exactly the "blocked on something only a human can supply" case** — I am stopping the data pull here rather than guessing at further credential permutations, and flagging it as AZ-005 below.

**No number in this report should be read as a fresh 2026-08-10 campaign read.** SP, SB, SD campaign performance and the SP search-term/targeting report were all planned but none could be requested successfully (the async report-request POST itself 401'd before a `reportId` was ever issued — there was nothing to poll or download).

## THE FOUR VERDICTS

### 1. CUT
**No fresh matured-window zero-sales campaign can be identified this session — the API that would show it is down.** The last confirmed matured-window CUT finding (2026-07-08) was: **"SafeBuds 3rd June Manual" — ₹4,600 spend, ₹0 attributed sales, 583 clicks → 5 conversions across wks 2–3, fully matured.** That finding is 5 weeks old, was drafted as AZ-001, and per tracker.md is **still `PENDING USER`** — unchanged in the interim (verified by reading the file this session). The number that matters this cycle is procedural, not fresh: **5 weeks of an undecided kill on a campaign already proven dead on a matured window.** Re-flagged as **AZ-006** below with an explicit ask to decide now, independent of this cycle's data blocker, since the original evidence doesn't go stale — only the exposure grows.

### 2. FIX
**The structural break this cycle is the Ads API authentication itself.** Grounded number: **0 of 6 live diagnostic Advertising API calls succeeded this session; 3 of them returned the identical explicit message `"clientId does not match token"`**, reproduced across both the EU and NA hosts, with and without the profile scope header, and in a same-command mint-and-use test that rules out staleness. Every downstream fix this department is supposed to make — negative keywords, harvest, bid tweaks, budget steps — is blocked behind this one broken credential pairing. Flagged as **AZ-005**, the top-priority item for Meet/IT this week: re-verify or regenerate the Ads API refresh token from the exact LWA Security Profile that owns `AMZADS_CLIENT_ID`.

### 3. SCALE or KILL
**No new scale/kill lever can be pulled this cycle — there is no matured data to judge marginal ROAS against.** What is gradable, though, is the department's own read discipline: **AZ-003 (SP-Comm 9th April'26 | Generic, the only campaign that has ever passed the scale test) was due a matured-window read on ~2026-07-19. It is now 2026-08-10 — 22 days past that date — with no read logged in learning-log.md and no status update in tracker.md.** That campaign is 53% of historical spend and 77% of historical attributed sales for this account; it is the single highest-value thing in the portfolio and it has gone three-plus weeks unread. Per this agent's own gate (2-week no-touch, then read before the next step), it should not receive another budget step until that overdue read happens — and that read cannot happen until AZ-005 is fixed. Flagged as **AZ-007**: highest-priority pull the moment API access is restored.

### 4. TEST
**New falsifiable hypothesis, queued for the first successful pull:** the SP search-term/targeting report for "SP-Comm 9th April'26 | Generic" will show **≥15% of its matured spend concentrated on 2 or fewer search terms/targets with zero attributed sales at ≥₹500 spend each.** Falsification: if spend is broadly spread across ≥10 converting terms with no single ₹500+ zero-sale term, the hypothesis is rejected and this campaign's ceiling is a creative/bid problem, not a targeting-hygiene one. This is genuinely net-new — tracker P1-1 (search-term harvest) has now been blocked for **two consecutive cycles on two different infrastructure problems** (Windsor table-sync in cycle 1, this API auth failure in cycle 2) — and it is a read-only, non-budget action, so it is never ratchet-gated even once access is restored. Logged to the scale-hypothesis backlog in learning-log.md. Flagged as **AZ-008**.

## GOVERNANCE NOTE

Per the stop list, no write call of any kind was attempted or considered — every call this session was a `GET`/`POST /auth/o2/token`/`POST /reporting/reports`, and every one of the reporting/profile calls failed at authentication before reaching any resource. There was never a point where a campaign, budget, bid, or account mutation was in reach.

## WHAT MEET NEEDS TO DO THIS WEEK

1. **Fix AZ-005** — this blocks everything else in this department until resolved. Confirm the LWA Security Profile behind `AMZADS_CLIENT_ID` actually owns the app that issued `AMZADS_REFRESH_TOKEN`, or regenerate the refresh token from the correct app.
2. **Decide AZ-001 (and by extension AZ-006)** — the SafeBuds 3rd June Manual kill has been sitting for 5 weeks on evidence that was already conclusive.
3. **Confirm whether AZ-002/AZ-003's July step was ever read outside this queue** (mirrors the pattern already seen twice in the Meta-ads lane, where changes were made manually and only logged after the fact) — if so, log the result so AZ-007 can close; if not, it's overdue now.

*Next cycle: retry the full pull immediately once AZ-005 is confirmed fixed — do not wait for the next scheduled week if the credential is fixed sooner, since every week lost here is a week the ₹6.0 floor and the department's ₹35L north star both go unmeasured.*
