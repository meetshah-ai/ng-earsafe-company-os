# Amazon Ads Operator — Weekly Cycle — 2026-08-13

**Accounts:** NG Corporation (Seller/SC, profile `1452172411967063`) · NG AMS (Vendor/VC, profile `1498424255518113`)
**Metric owned by this agent:** ad-attributed ROAS (14-day attribution, matured window) ≥ **6.0**, per campaign, rolled up by ad type and account. **This is NOT the department's blended P&L ROAS ≥15 bar** (SC+VC marketplace revenue ÷ total ad spend, owned by `amazon.md`/`constitution.md`) — the two are never interchangeable and neither number below substitutes for the other.
**Matured-window cutoff today:** anything after **2026-07-30** is inside the 14-day attribution lag and cannot be used for a kill/scale call even if it existed.

## Session result: BLOCKED — 3rd consecutive live-tested identical auth failure

This cycle ran the full live sequence against the real Amazon Ads API (no Windsor, no MCP shortcut):

1. `POST https://api.amazon.com/auth/o2/token` → **HTTP 200**, valid `access_token` (787 chars, `Atza|`-format). Refresh token + client secret are a valid pair — confirmed again.
2. Canary `GET /v2/profiles`, `advertising-api-eu.amazon.com`, no scope header → **HTTP 401** `{"code":"UNAUTHORIZED"}`.
3. `GET /v2/profiles` with `Amazon-Advertising-API-Scope: 1498424255518113` (NG AMS) → **HTTP 401** `UNAUTHORIZED`.
4. `GET /v2/profiles` with `Amazon-Advertising-API-Scope: 1452172411967063` (NG Corporation) → **HTTP 401** `UNAUTHORIZED`.
5. `POST /reporting/reports` (real `spCampaigns` body, `groupBy` included, NG AMS scope) → **HTTP 401** `{"message":"Unauthorized exception while handling 3P Request: clientId does not match token"}`.
6. Same-command fresh-mint-and-use retest — new token minted in the same script, immediately used against `POST /reporting/reports` (NG Corporation scope) → **HTTP 401**, identical `clientId does not match token` message.

**0 of 5 resource calls succeeded, on both profiles, on both an older and a freshly-minted token.** This is the exact same failure signature logged 2026-08-10 and earlier on 2026-08-13 (see `learning-log.md`) — meaning **this is now the 3rd consecutive live-tested cycle** with an identical result. No SP/SB/SD campaign report, no search-term/targeting report, and no `reportId` was ever issued this session either. This department's own 2026-08-10 rule says a 3rd identical failure means stop re-deriving the diagnosis and treat it as a pure IT ticket — that rule is now invoked; no host/scope/staleness permutations were re-tested beyond the minimum needed to confirm the signature was unchanged (already ruled out twice before: EU vs NA host, scope header presence, token staleness).

**Everything below this line is therefore built from the last confirmed data (2026-07-08 baseline, now 36 days old) plus this session's own auth-failure facts — not a fresh pull.** Anything carried forward is explicitly labeled stale; nothing stale is used to justify a NEW budget action, only to reaffirm holds/cuts already queued.

---

## VERDICT 1 — CUT: SafeBuds 3rd June Manual (reaffirm, escalated)

**Number, unrefuted for a 3rd straight cycle:** matured weeks 2–3 of the 2026-07-08 pull — **₹4,600 spend → ₹0 attributed sales, 583 clicks, 5 conversions** (conversions did not convert to attributed sales in-window — a fully matured zero). Ad-attributed ROAS = **0.0**, nowhere near the 6.0 floor.
This evidence has now sat undecided for **36 days** with zero ability for this agent to refute or update it (API blocked all 3 attempts to refresh it). Estimated further waste at the campaign's own last-observed ~₹2,300/wk burn: **≈₹12,000–13,000 accrued since the original read**, total plausible exposure **≈₹16,000–18,000 at 0% conversion** — an estimate, not a fresh figure, carried unchanged from AZ-010 because no newer spend data exists.
**Draft:** AZ-014, superseding AZ-010 — escalates the ask from "flag for decision" to "act now, do not wait for a 4th confirmation."

## VERDICT 2 — FIX: the credential/Security-Profile mismatch is the structural break

**Number from this session:** 5/5 diagnostic calls failed identically — both profiles, both an existing and a freshly-minted token, one canary + one real report POST + one mint-and-use retest. Every failure carries the message `"clientId does not match token"` (or the generic 401 on the un-scoped canary). Token-mint success + universal resource-call rejection is the fingerprint of `AMZADS_CLIENT_ID` not being paired with the same LWA Security Profile that issued `AMZADS_REFRESH_TOKEN`, or that Security Profile lacking Advertising API scope — unchanged diagnosis from 2026-08-10, now confirmed a 3rd time. This is the single structural thing breaking every other line of work in this department (P0-1/P0-2/P0-3 refresh, P1-1 harvest, SKU coverage map) — nothing else can be fixed until this is.
**Draft:** AZ-013, superseding AZ-009 — final escalation tier: recommend Meet/IT treat this as a credential-rotation ticket (reissue `AMZADS_CLIENT_ID`/`AMZADS_CLIENT_SECRET`/`AMZADS_REFRESH_TOKEN` as one matched set with Advertising API scope granted), not a re-diagnosis task — this agent has exhausted what it can test from this side.

## VERDICT 3 — SCALE-or-KILL: Comm Generic — HOLD (reaffirmed, no new action possible)

**Number, last confirmed 2026-07-08:** "SP-Comm 9th April'26 | Generic" — 30-day matured ₹35.2K spend → ₹1.10L attributed sales (**4.5× ad-attributed ROAS**), 53% of SP spend, 77% of attributed sales, wk3→wk4 +144% spend held ROAS in range. This is the only campaign that has ever passed the scale test.
**Gate check:** the scale gate requires matured-window evidence AND a 2-week no-touch window after any step — this evidence is now **36 days stale**, and the campaign's stated read date is **25+ days overdue**. Stale evidence does not satisfy the gate for a NEW increment, no matter how strong it looked when fresh. **No budget step is proposed this cycle, in either direction** — this remains an explicit HOLD, not a silent drop.
**Draft:** AZ-015, superseding AZ-011 — same hold, reaffirmed; will only change on the first successful matured pull.

## VERDICT 4 — TEST: SafeBuds demand-vs-funding mismatch (net-new hypothesis)

**Hypothesis:** once API access is restored, SafeBuds-tagged SP campaigns will show matured ad-attributed ROAS ≥ 6.0 — i.e., the constraint on SafeBuds is funding, not demand, because SC-side SafeBuds demand was accelerating **+77% WoW** as of the 2026-07-08 baseline while "SafeBuds 5th May" (5.19× ROAS in week 1) was defunded and "SafeBuds 3rd June Manual" kept burning at 0×.
**Falsification setup:** on the first successful pull, compare SafeBuds campaign(s) matured 14-day ROAS and spend trend against the SP account average. **Threshold:** ROAS ≥ 6.0 held for **2 consecutive matured weeks**, at the smallest read size available (~₹3,000–5,000 of matured spend), before any budget step is proposed — mirrors the same-gate discipline applied to Comm Generic.
**Draft:** AZ-016 (net-new) — added to the scale-hypothesis backlog; distinct from the general search-term harvest hypothesis already queued (AZ-008), scoped specifically to the SafeBuds angle.

---

## Non-budget optimization (never gated by the ratchet) — status this cycle

**Not actionable this session — no data.** The SP search-term/targeting harvest (P1-1) requires the same blocked report pipeline; this is now its **4th consecutive missed attempt** across 3 differently-dated cycles (2026-07-08: Windsor sync backlog; 2026-08-10: API auth; 2026-08-13 (earlier session): API auth; 2026-08-13 (this session): API auth again). AZ-008 remains queued exactly as drafted — no new negatives or harvest candidates can be named without real search-term rows, and fabricating example terms would violate this agent's own no-assumption rule on field names and data. This is flagged, not silently dropped.

## Structural flags still open (unchanged, no new evidence)

- **NG AMS account + all 14 SB campaigns — 100% dark** (zero impressions) as of the last read (AZ-004, still pending). Cannot confirm this session whether it's still true, resolved, or worse — it needs the same blocked pipeline. Continues to be treated as an account/billing investigation, not a performance verdict, per this agent's own gate for dark ad types.
- **AZ-002 (SafeBuds 5th May relaunch)** — still pending a human decision, unrelated to the API block (it was a queued RELAUNCH ask, not a data-dependent one).

## Escalation status

Amazon Ads API authentication is now broken across **3 consecutive live-tested cycles** with an **identical, specific error** (`clientId does not match token`), on both profiles, with both an existing and a freshly-minted token. This is no longer an open diagnostic question from this agent's side — every permutation this agent can test (host, scope header, token freshness, profile) has been exhausted and ruled out twice over. **This is now purely an IT/credential-provisioning ticket** (AZ-013): re-verify or reissue `AMZADS_CLIENT_ID` / `AMZADS_CLIENT_SECRET` / `AMZADS_REFRESH_TOKEN` as one matched LWA Security Profile with Advertising API scope granted.

## No mutating calls made

Only `POST /auth/o2/token` (token exchange, not an Ads API mutation), `GET /v2/profiles` ×3, and `POST /reporting/reports` ×2 (report *requests*, which 401'd before any report was ever created) were called this session. No campaign, ad group, keyword, budget, or bid mutation was attempted or possible.
