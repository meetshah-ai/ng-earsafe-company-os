# NG EarSafe — Slack relay environment variables (multi-agent convening model)
# Copy the VALUES into your host's dashboard (Railway/Render) -> Variables.
# The agent/channel ROUTING now lives in routes.json (copy routes.example.json), NOT here —
# only true secrets belong in env. Never commit a filled-in copy of this file.

# --- SECRETS (you fill these in) ---
SLACK_SIGNING_SECRET=            # Slack app -> Basic Information -> Signing Secret
SLACK_BOT_TOKEN=                 # Slack app -> OAuth & Permissions -> Bot User OAuth Token (xoxb-…)
ANTHROPIC_API_KEY=               # console.anthropic.com -> API Keys (sk-ant-…)

# --- ALREADY FILLED (NG EarSafe shared resources — safe; the read-only vault is the safety net) ---
ENVIRONMENT_ID=env_01XyAqVA96XbV6YDd2YpeyPB    # ng-eaarsafe (shared by all agents)
VAULT_ID=vlt_011Cct4FFqcsnrcEDFfzHCXo          # ng-earsafe-readonly (READ-ONLY — never a write vault)

# Agent + channel routing is in routes.json (see routes.example.json).
