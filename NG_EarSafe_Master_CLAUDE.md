# NG EarSafe — Master Operating Brief (moved)

> **This content now lives in the auto-loaded `CLAUDE.md` at the git root** — that is the file Claude Code reads automatically every session, so the master brief belongs there.
>
> - Company frame, north star, the AI-first spine, read-order, and the "which doc to load" role table → **`CLAUDE.md`** (git root).
> - Single source of truth (products, finance/supply, channel truths, cross-department gates, connectors) → **`Company_OS/COMPANY_STATE.md`**.
>
> This stub is kept only so older links don't dangle. Do not add content here.
